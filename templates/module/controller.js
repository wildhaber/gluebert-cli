import { ControllerAbstract } from 'gluebert/controller';

/**
 * Class represents $NAME_UPPER_CAMEL$Controller
 * @extends ControllerAbstract
 */
class $NAME_UPPER_CAMEL$Controller extends ControllerAbstract {

    /**
     * creates a new $NAME_UPPER_CAMEL$Controller instance
     * @param {HTMLElement} element
     * @param {DataObserver} data
     * @param {ElementBuilder} elements
     * @param {DependencyManager} dependencies
     */
    constructor(element = null, data, elements, dependencies) {
        super(element, data, elements, dependencies);
    }

}

export {
    $NAME_UPPER_CAMEL$Controller,
};